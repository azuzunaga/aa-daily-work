class TowersOfHanoi {
  run() {
    //Until game won? (Three disks on one tower)
      //Read move from player
        //if their move is valid,
          //Make the move on the towers
        //Otherwise, prompt to make another move
  }
}
